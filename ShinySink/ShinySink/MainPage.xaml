<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ShinySink.MainPage"
             Title="Shiny Sink">

    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Label Text="Fire Hey Stupid?" />
                <Switch IsToggled="{Binding FireHeyStupid}" />

                <Button Text="{Binding JobPermissionText, StringFormat='Job Permissions: {0}'}"
                        Command="{Binding JobPermissions}" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>
